<!doctype html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="global.css">
  <script id="scoreTemplate" type="text/ractive">
    {{#winner}}
      <div>
        <h2 class="winner winner-name">{{winner}}</h2>
      </div>
    {{/winner}}
    {{#started}}
      <span class="vs">
        {{score.a}} - {{score.b}}
      </span>
    {{/started}}
  </script>
  <script id="adminTemplate" type="text/ractive">
    <ul>
      {{^players}}
        <li>no players</li>
      {{/started}}

      {{#each players:num}}
        <li><input type="checkbox" on-click="select:{{num}}">{{name}}</li>
      {{/each}}
    </ul>

    {{^started}}
      <button class="button" on-click="action:'start'">Start</button>
    {{/started}}

    {{#started}}
      <button class="button" on-click="action:'stop'">Stop</button>
    {{/started}}
  </script>
</head>
  <body>
    <script src="console.js"></script>
    <script src="jquery.min.js"></script>
    <script src="ractive.min.js"></script>
    <script src="api.js"></script>
    <script src="es.js"></script>
    <%- yield %>
    <script>ES.connect();</script>
  </body>
</html>
